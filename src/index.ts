export { log, group, badge, banner, logo, combo, createLogger } from "./core";
export { presets } from "./presets";
export type {
  FancyComboOptions,
  FancyLogOptions,
  FancyLogger,
  FancyLogFunction,
  FancyGroupFunction,
  FancyLogPreset,
} from "./types";
