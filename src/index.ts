export { log, group, badge, banner, logo, createLogger } from "./core";
export { presets } from "./presets";
export type {
  FancyLogOptions,
  FancyLogger,
  FancyLogFunction,
  FancyGroupFunction,
  FancyLogPreset,
} from "./types";
