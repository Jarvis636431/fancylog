# TODO

## 高优先级
- [ ] `combo` 支持自定义 `label/text` 样式（保留简写模式）
- [ ] `theme` 不存在时的 fallback 处理（避免静默失败）
- [ ] `multi` 支持 `theme` 简写（`{ text, theme: "badge" }`）

## 中优先级
- [ ] `registerTheme` 防覆盖内置主题（或要求显式覆盖）
- [ ] `createLogger` 支持默认 `theme`（统一风格更方便）
- [ ] `ascii` 图标支持 `variant` 或 `size`（compact/large）

## 工程/发布
- [ ] `exports` 明确命名导出说明（可选）
- [ ] 补充发布清单（版本号/发布命令/标签）
